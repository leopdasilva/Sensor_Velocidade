CREATE DATABASE IF NOT EXISTS projeto_esp32;

USE projeto_esp32;

CREATE TABLE IF NOT EXISTS dados_sensor (
    id INT AUTO_INCREMENT PRIMARY KEY,
    velocidade FLOAT NOT NULL,
    data_hora DATETIME DEFAULT CURRENT_TIMESTAMP
);
